## Arduino 與使用者行為交互

1. Mode1 鏡子模式
2. Mode2 開啟畫面
3. Mode3 開啟偏見回顧報告

## Page logic

```js
function handleMirrorInteraction(weatherState, rainDuration, buttonPressed) {
  if (
    (weatherState === "小雨" || weatherState === "暴雨") &&
    rainDuration > 120
  ) {
    playCrackAnimation(); // 播放裂痕動畫
  } else if (buttonPressed) {
    openBiasRecordPage(); // 打開偏見事件紀錄頁面
  } else if (weatherState === "晴天" || weatherState === "陰天") {
    keepUnchanged(); // 不變化
  } else {
    console.warn("未預期的輸入狀況");
  }
}

// 範例功能函數（請根據實際實作替換）
function playCrackAnimation() {
  console.log("播放裂痕動畫");
}

function openBiasRecordPage() {
  console.log("前往偏見事件紀錄頁面");
}

function keepUnchanged() {
  console.log("鏡子畫面不變化");
}
```
